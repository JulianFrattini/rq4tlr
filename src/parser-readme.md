# Use Case Parser

This subdirectory contains the source code that parses the raw data, formats them, and stores them in a common, reusable format.

## Structure



## Usage

To execute the parser, run the following command in an environment that has all `requirements.txt` installed (where `<name>` is replaced with the name of a dataset):

```
python .\parse.py --dataset <name>
```

## Development